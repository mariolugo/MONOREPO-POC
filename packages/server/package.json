{
  "name": "@belong/server",
  "version": "1.0.0",
  "private": true,
  "description": "Mock server for the belong API",
  "main": "./lib/index.js",
  "types": "./lib/index.d.ts",
  "files": [
    "lib/**"
  ],
  "scripts": {
    "build": "tsc -b",
    "watch": "tsc --watch",
    "lint": "eslint src --ext .ts --max-warnings 0"
  },
  "dependencies": {
    "faker": "^5.5.3",
    "msw": "^0.28.2"
  },
  "devDependencies": {
    "@belong/client": "*"
  }
}
